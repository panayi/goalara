%ul{:id => 'updated'}
  - @articles.each do |article|
    %li
      = link_to image_tag(article.image), article.url
      %h3
        = article.title
      %span
        = truncate_words(article.summary, 200).join(' ')

